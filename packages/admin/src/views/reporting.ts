export function reportingContent(): string {
  return `
    <section class="page-section reporting-page">
      <h2>Reporting</h2>
      <p class="sub page-lead">Filter game sessions by setup and see outcome totals, unique players, and breakdowns.</p>
      <div id="reporting-host"><div class="muted">Loading report…</div></div>
    </section>`;
}
