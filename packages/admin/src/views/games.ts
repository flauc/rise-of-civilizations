export function gamesContent(): string {
  return `
    <section class="page-section games-page">
      <h2>Games played</h2>
      <p class="sub page-lead">Recent sessions — filter by player, mode, or result.</p>
      <div id="games-table-host"><div class="muted">Loading games…</div></div>
    </section>`;
}
